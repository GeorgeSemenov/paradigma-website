mixin contact(modifier)
  if modifier == undefined
    - modifier = {}
  if modifier["href"] == undefined
    - modifier["href"] = "#!";
  if modifier["text"] == undefined
    - modifier["text"] = "empty__linktext";
  a.contact(href=modifier.href)&attributes(attributes)!=modifier.text
